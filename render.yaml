services:
  - type: web
    name: study-room-frontend
    env: static
    region: oregon
    branch: main
    buildCommand: echo "Static frontend - no build step required"
    staticPublishPath: public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
                  # Free tier: 1GB storage, 90-day limit
